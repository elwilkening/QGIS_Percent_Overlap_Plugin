[general]
name=Temporal Overlap Calculator
qgisMinimumVersion=3.0
description=Calculate percentage overlap between an input layer and temporal overlay layer(s) by year. The plugin accounts for overlapping features in overlay layers (counted only once) and outputs results to CSV.
version=1.0.0
author=Development Team
email=dev@example.com
url=https://example.com
repository=https://github.com/example/overlap
tracker=https://github.com/example/overlap/issues
icon=icon.png
tags=overlay,temporal,overlap,intersection,analysis
category=Analysis
experimental=False
deprecated=False

[dependencies]
QGIS>=3.0

[changelog]
1.0.0 - Initial release
- Temporal overlay calculation by year
- Support for multiple date/time formats
- Overlapping feature deduplication via union
- CSV export with nm² conversion
